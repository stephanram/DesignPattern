using System;
using DesignPattern.Pattern;
using DesignPattern.Pattern.AbstractFactory;

namespace DesignPattern
{
    class Program
    {
        static void Main(string[] args)
        {
            //Singleton.Init();

            //FactoryDesign.Init();

            //FactoryMethodDesign.Init();

            AbstractFactoryMethod.Init();

            Console.ReadLine();
        }
    }
}
